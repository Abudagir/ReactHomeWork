import "./App.css";
import Counter from "./counter/Counter";

// export const Context = createContext(null);

function App() {
  return (
    <div>
      <Counter/>
    </div>
  );
}

export default App;
